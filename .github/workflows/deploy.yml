name: Deploy to EC2

on:
  push:
    branches:
      - main

concurrency:
  group: deploy-mano-attendance
  cancel-in-progress: false

jobs:
  build-assets:
    name: Build & Verify (CI)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: |
            package-lock.json
            backend/package-lock.json
            frontend/package-lock.json

      - name: Install Root Dependencies
        run: npm install --no-audit --no-fund

      - name: Install Backend Dependencies
        run: |
          cd backend
          npm install --no-audit --no-fund

      - name: Install Frontend Dependencies
        run: |
          cd frontend
          npm install --no-audit --no-fund

      - name: Build Frontend
        env:
          VITE_RECAPTCHA_SITE_KEY: "6Lf49jksAAAAAG9BCGdoBfzCSJZF72EEEAn-ue0b"
          VITE_ENABLE_CAPTCHA: "true"
          # VITE_BACKEND_URL points to the backend API/socket origin.
          # The frontend socket will connect to this URL directly,
          # bypassing the Nginx static file server to avoid WebSocket proxy issues.
          VITE_BACKEND_URL: "https://attendance.mano.co.in"
        run: |
          cd frontend
          npm run build

      - name: Upload Frontend Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: frontend/dist

  deploy-to-ec2:
    name: Deploy via Self-Hosted Runner
    needs: build-assets
    runs-on: [self-hosted, ec2-prod]
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Download Frontend Build Artifact
        uses: actions/download-artifact@v4
        with:
          name: frontend-build
          path: frontend/dist

      - name: Sync Production Codebase
        run: |
          echo "📥 Updating production repository codebase..."
          cd /var/www/MANO-HRM-Web || exit
          sudo git fetch origin main
          sudo git reset --hard origin/main

      - name: Deploy Backend & Restart PM2
        run: |
          echo "⚙ Setting up Backend in production directory..."
          cd /var/www/MANO-HRM-Web/backend
          sudo npm install --production --no-audit --no-fund
          
          # Restart backend process using PM2
          if sudo pm2 describe "MANO-Attendance" > /dev/null 2>&1; then
            echo "🔄 Reloading backend with PM2..."
            sudo pm2 reload "MANO-Attendance" --update-env
          elif pm2 describe "MANO-Attendance" > /dev/null 2>&1; then
            echo "🔄 Reloading backend with PM2..."
            pm2 reload "MANO-Attendance" --update-env
          else
            echo "🚀 Starting new backend process under PM2..."
            sudo pm2 start server.js --name "MANO-Attendance" --update-env
          fi

      - name: Deploy Frontend to Nginx Target
        run: |
          echo "📁 Copying pre-built assets to production dist..."
          sudo mkdir -p /var/www/MANO-HRM-Web/frontend/dist
          sudo cp -r $GITHUB_WORKSPACE/frontend/dist/* /var/www/MANO-HRM-Web/frontend/dist/

          # Validate and reload Nginx
          if sudo nginx -t; then
            sudo systemctl reload nginx
            echo "✅ Nginx reloaded successfully."
          else
            echo "❌ Nginx config test failed."
            exit 1
          fi
          echo "🎉 Deployment successfully completed!"
